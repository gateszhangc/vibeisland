apiVersion: v1
kind: Service
metadata:
  name: vibeisland
  labels:
    app.kubernetes.io/name: vibeisland
    app.kubernetes.io/part-of: vibeisland
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vibeisland
  ports:
    - name: http
      port: 80
      targetPort: 3000
